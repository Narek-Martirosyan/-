

export const showTodayHandler = (showToday, setShowToday) => {
    if (showToday) {
        setShowToday(!showToday);
    } else {
        setShowToday(!showToday);
    }
}

export const showOneHandler = (showOne, setShowOne) => {
    if (showOne) {
        setShowOne(!showOne);
    } else {
        setShowOne(!showOne);
    }
}

export const showTwoHandler = (showTwo, setShowTwo) => {
    if (showTwo) {
        setShowTwo(!showTwo);
    } else {
        setShowTwo(!showTwo);
    }
}

export const showThreeHandler = (showThree, setShowThree) => {
    if (showThree) {
        setShowThree(!showThree);
    } else {
        setShowThree(!showThree);
    }
}

export const showFourHandler = (showFour, setShowFour) => {
    if (showFour) {
        setShowFour(!showFour);
    } else {
        setShowFour(!showFour);
    }
}

export const showFiveHandler = (showFive, setShowFive) => {
    if (showFive) {
        setShowFive(!showFive);
    } else {
        setShowFive(!showFive);
    }
}
